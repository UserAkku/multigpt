export type Page = "overview" | "chats" | "knowledge" | "decisions" | "tasks" | "files" | "activity" | "members";
export type Person = { id: string; name: string; initials: string; tone: string };
export type Message = { id: string; role: "user" | "assistant" | "system"; content: string; authorId?: string; createdAt: string; comments: Comment[]; sources?: Source[] };
export type Comment = { id: string; authorId: string; content: string; createdAt: string };
export type Source = { kind: "decision" | "knowledge" | "conversation"; id: string; label: string };
export type Conversation = { id: string; title: string; preview: string; createdById: string; updatedAt: string; branchName?: string; parentId?: string; messages: Message[]; participants: string[]; provider: string; model: string };
export type Knowledge = { id: string; title: string; content: string; type: "finding" | "research" | "requirement" | "note" | "fact"; authorId: string; createdAt: string; sourceConversationId?: string; sourceMessageId?: string };
export type Decision = { id: string; title: string; description: string; reason: string; alternatives: string; status: "proposed" | "under_review" | "confirmed" | "rejected" | "archived"; authorId: string; createdAt: string; sourceConversationId?: string; sourceMessageId?: string };
export type Task = { id: string; title: string; description: string; status: "todo" | "in_progress" | "done"; priority: "low" | "medium" | "high"; authorId: string; assigneeId?: string; createdAt: string; sourceConversationId?: string; sourceDecisionId?: string };
export type Activity = { id: string; actorId: string; action: string; target: string; at: string; kind: "chat" | "knowledge" | "decision" | "task" | "people" | "file" };
export type FileItem = { id: string; name: string; size: number; type: string; createdAt: string; uploaderId: string; url?: string };
export type Project = { id: string; name: string; description: string; createdAt: string; members: Person[]; conversations: Conversation[]; knowledge: Knowledge[]; decisions: Decision[]; tasks: Task[]; activity: Activity[]; files: FileItem[] };
export type AppData = { projects: Project[]; currentUserId?: string };
